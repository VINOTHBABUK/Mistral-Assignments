#include <stdio.h>
#include "header.h"
int main(){
   int a,b;
   scanf("%d %d",&a,&b);
     printf("\nWelcome to my program world");
     add(a,b);
    printf("\nAddition file is ended");
    subtraction(a,b);
   printf("\nsubtraction file is ended");
   multiplication(a,b);
   printf("\nMultiplication file is ended");
   division(a,b);
  printf("\nDivision file is  ended");
  printf("\nMain file is ended\n");
}

